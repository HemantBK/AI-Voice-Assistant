# Source of truth for dependencies is pyproject.toml.
# This file is kept for the Docker build and for users who prefer
# `pip install -r requirements.txt`. It installs the full runtime stack
# (audio + LLM providers + observability). CI and lean local setups
# should use pyproject extras directly:
#     pip install -e ".[dev,observability]"
-e .[llm,audio,observability]
